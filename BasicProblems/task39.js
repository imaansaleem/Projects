function city_country(name_of_city, country){
    let message=name_of_city+", "+country;
    return message;
}

let pairs;
pairs=city_country("Dubai", "United Arab Emirates");
console.log(pairs);
pairs=city_country("Lahore", "Pakistan");
console.log(pairs)
pairs=city_country("Bradford", "England");
console.log(pairs)
